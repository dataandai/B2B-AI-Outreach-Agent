version: '3.9'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///./customer_agent.db
      ENABLE_OPENAI: "false"
    volumes:
      - ./:/app
